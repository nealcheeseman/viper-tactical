let p=null,m=JSON.parse(localStorage.getItem("marks")||"{}");let t=300,it=null;const g=i=>document.getElementById(i);function renderTimer(){g('timer').innerHTML=String(Math.floor(t/60)).padStart(2,'0')+':'+String(t%60).padStart(2,'0')}function d(a,b,c,d2){const R=6371000;let x=(d2-b)*Math.PI/180*Math.cos((a+c)/2*Math.PI/360),y=(c-a)*Math.PI/180;return Math.sqrt(x*x+y*y)*R}function restore(){['boat','pin','top'].forEach(k=>{g(k+'Btn').classList.remove('ok');if(m[k])g(k+'Btn').classList.add('ok')})}function save(){localStorage.setItem('marks',JSON.stringify(m));restore();upd()}g('gpsBtn').onclick=()=>navigator.geolocation.watchPosition(x=>{p=x.coords;g('gps').innerHTML='GPS ✅';g('gpsBtn').classList.add('ok');upd();},e=>alert(e.message),{enableHighAccuracy:true});function ping(k){if(!p)return alert('Start GPS first');m[k]={lat:p.latitude,lon:p.longitude};save()}boatBtn.onclick=()=>ping('boat');pinBtn.onclick=()=>ping('pin');topBtn.onclick=()=>ping('top');clearBtn.onclick=()=>{m={};localStorage.removeItem('marks');restore();g('metrics').innerHTML='';};startBtn.onclick=()=>{clearInterval(it);t=parseInt(seq.value)*60;renderTimer();it=setInterval(()=>{if(t<=0){renderTimer();clearInterval(it);return;}t--;renderTimer();},1000)};syncBtn.onclick=()=>{t=Math.floor(t/60)*60;renderTimer()};resetBtn.onclick=()=>{clearInterval(it);t=parseInt(seq.value)*60;renderTimer()};function upd(){if(!p)return;let wd=parseFloat(wind.value)||220;let hd=p.heading||0;let tack=((hd-wd+360)%360<180)?'STARBOARD':'PORT';let txt='';if(m.boat&&m.pin){let bx=m.boat.lat,by=m.boat.lon,px=m.pin.lat,py=m.pin.lon;let vx=px-bx,vy=py-by,wx=p.latitude-bx,wy=p.longitude-by;let area=Math.abs(vx*wy-vy*wx);let distLine=(area/Math.sqrt(vx*vx+vy*vy))*111111;let eta=(p.speed||0)>0?distLine/(p.speed||1):0;let burn=t-eta;txt+='To Line '+distLine.toFixed(0)+'m<br>ETA '+eta.toFixed(0)+'s<br>Burn '+burn.toFixed(0)+'s<br>';}g('race').innerHTML=tack;g('metrics').innerHTML=txt;}restore();renderTimer();