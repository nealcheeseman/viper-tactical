let p=null,cal=false,pts=[],wind=null;let port=[],star=[];const g=i=>document.getElementById(i);function bearing(a,b,c,d){let y=Math.sin((d-b)*Math.PI/180)*Math.cos(c*Math.PI/180);let x=Math.cos(a*Math.PI/180)*Math.sin(c*Math.PI/180)-Math.sin(a*Math.PI/180)*Math.cos(c*Math.PI/180)*Math.cos((d-b)*Math.PI/180);return (Math.atan2(y,x)*180/Math.PI+360)%360;}function avg(a){return a.length?a.reduce((x,y)=>x+y,0)/a.length:null;}function refresh(){if(wind!=null)g('wind').innerHTML='WIND: '+Math.round(wind)+'°';g('portAvg').innerHTML='PORT AVG: '+(avg(port)?Math.round(avg(port))+'°':'--');g('starAvg').innerHTML='STAR AVG: '+(avg(star)?Math.round(avg(star))+'°':'--');g('samples').innerHTML='SAMPLES: '+(port.length+star.length);let conf='LOW';if(wind!=null) conf='MEDIUM';if(port.length>5&&star.length>5) conf='HIGH';g('conf').innerHTML='CONFIDENCE: '+conf; if(port.length>2&&star.length>2){wind=((avg(port)+avg(star))/2+360)%360;} }
gpsBtn.onclick=()=>navigator.geolocation.watchPosition(pos=>{p=pos.coords;g('gps').innerHTML='GPS ✅';if(cal){pts.push([p.latitude,p.longitude]);g('status').innerHTML='Calibrating '+pts.length+'/10';if(pts.length>=10){let br=[];for(let i=1;i<pts.length;i++)br.push(bearing(pts[i-1][0],pts[i-1][1],pts[i][0],pts[i][1]));let cog=avg(br);wind=(cog+180)%360;cal=false;g('status').innerHTML='Calibration Complete';g('diag').innerHTML='COG '+Math.round(cog)+'°';}} if(wind!=null && p.heading!=null){let rel=(p.heading-wind+360)%360;if(rel<180) star.push(p.heading); else port.push(p.heading); if(star.length>30)star.shift(); if(port.length>30)port.shift();} refresh();},e=>alert(e.message),{enableHighAccuracy:true});
calBtn.onclick=()=>{if(!p)return alert('Start GPS first');pts=[];cal=true;g('status').innerHTML='Sail downwind...';};