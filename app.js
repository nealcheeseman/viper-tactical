let p=null,cal=false,pts=[],wind=null;let port=[],star=[];const g=i=>document.getElementById(i);function avg(a){return a.length?a.reduce((x,y)=>x+y,0)/a.length:null}function bearing(a,b,c,d){let y=Math.sin((d-b)*Math.PI/180)*Math.cos(c*Math.PI/180);let x=Math.cos(a*Math.PI/180)*Math.sin(c*Math.PI/180)-Math.sin(a*Math.PI/180)*Math.cos(c*Math.PI/180)*Math.cos((d-b)*Math.PI/180);return (Math.atan2(y,x)*180/Math.PI+360)%360;}function draw(){if(wind!=null)g('wind').innerHTML='WIND '+Math.round(wind)+'°';let conf='LOW';if(wind!=null)conf='MEDIUM';if(port.length>5&&star.length>5)conf='HIGH';g('conf').innerHTML='CONFIDENCE '+conf;g('portAvg').innerHTML='PORT AVG '+(avg(port)?Math.round(avg(port))+'°':'--');g('starAvg').innerHTML='STAR AVG '+(avg(star)?Math.round(avg(star))+'°':'--');g('samples').innerHTML='SAMPLES '+(port.length+star.length);}gpsBtn.onclick=()=>navigator.geolocation.watchPosition(pos=>{p=pos.coords;g('gps').innerHTML='GPS ✅';if(cal){pts.push([p.latitude,p.longitude]);g('status').innerHTML='Calibrating '+pts.length+'/10';if(pts.length>=10){let br=[];for(let i=1;i<pts.length;i++)br.push(bearing(pts[i-1][0],pts[i-1][1],pts[i][0],pts[i][1]));wind=(avg(br)+180)%360;cal=false;g('status').innerHTML='Calibration Complete';}}let track=p.heading; if(wind!=null && track!=null){g('track').innerHTML='TRACK '+Math.round(track)+'°';let rel=(track-wind+360)%360;let target;if(rel<180){star.push(track);if(star.length>30)star.shift();g('tack').innerHTML='STARBOARD';target=avg(star);}else{port.push(track);if(port.length>30)port.shift();g('tack').innerHTML='PORT';target=avg(port);} if(target){let d=track-target;g('delta').innerHTML='DELTA '+Math.round(d)+'°';if(d>3){g('lift').innerHTML='LIFT +'+Math.round(d)+'°';g('lift').className='hero2 good';}else if(d<-3){g('lift').innerHTML='HEADER '+Math.round(d)+'°';g('lift').className='hero2 bad';}else{g('lift').innerHTML='ON TARGET';g('lift').className='hero2';}}} draw();},e=>alert(e.message),{enableHighAccuracy:true});calBtn.onclick=()=>{if(!p)return alert('Start GPS first');pts=[];cal=true;g('status').innerHTML='Sail downwind...';};