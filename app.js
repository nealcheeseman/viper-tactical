let watch=null,p=null,pts=[],cal=false;const g=id=>document.getElementById(id);function bearing(a,b,c,d){let y=Math.sin((d-b)*Math.PI/180)*Math.cos(c*Math.PI/180);let x=Math.cos(a*Math.PI/180)*Math.sin(c*Math.PI/180)-Math.sin(a*Math.PI/180)*Math.cos(c*Math.PI/180)*Math.cos((d-b)*Math.PI/180);return (Math.atan2(y,x)*180/Math.PI+360)%360;}gpsBtn.onclick=()=>{watch=navigator.geolocation.watchPosition(pos=>{p=pos.coords;g('gps').innerHTML='GPS ✅';if(cal){pts.push([p.latitude,p.longitude]);g('status').innerHTML='Calibrating... '+pts.length+'/10';if(pts.length>=10){cal=false;let br=[];for(let i=1;i<pts.length;i++)br.push(bearing(pts[i-1][0],pts[i-1][1],pts[i][0],pts[i][1]));let cog=br.reduce((a,b)=>a+b,0)/br.length;let wind=(cog+180)%360;g('wind').innerHTML='WIND: '+Math.round(wind)+'°';g('conf').innerHTML='CONFIDENCE: MEDIUM';g('status').innerHTML='Calibration Complete';g('diag').innerHTML='COG: '+Math.round(cog)+'°<br>Wind: '+Math.round(wind)+'°<br>Samples: '+pts.length;}}},e=>alert(e.message),{enableHighAccuracy:true});};calBtn.onclick=()=>{if(!p)return alert('Start GPS first');pts=[];cal=true;g('status').innerHTML='Sail directly downwind...';};