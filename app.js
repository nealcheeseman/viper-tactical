let p=null,cal=false,pts=[],wind=null;let port=[],star=[];const g=i=>document.getElementById(i);function avg(a){return a.length?a.reduce((x,y)=>x+y,0)/a.length:null;}function br(a,b,c,d){let y=Math.sin((d-b)*Math.PI/180)*Math.cos(c*Math.PI/180);let x=Math.cos(a*Math.PI/180)*Math.sin(c*Math.PI/180)-Math.sin(a*Math.PI/180)*Math.cos(c*Math.PI/180)*Math.cos((d-b)*Math.PI/180);return (Math.atan2(y,x)*180/Math.PI+360)%360;}function redraw(){if(wind!=null)g('wind').innerHTML='WIND '+Math.round(wind)+'°';g('port').innerHTML='PORT AVG '+(avg(port)?Math.round(avg(port))+'°':'--');g('star').innerHTML='STAR AVG '+(avg(star)?Math.round(avg(star))+'°':'--');g('samples').innerHTML='SAMPLES '+(port.length+star.length);let c='LOW';if(wind!=null)c='MEDIUM';if(port.length>5&&star.length>5)c='HIGH';g('confidence').innerHTML='CONFIDENCE '+c;}gpsBtn.onclick=()=>navigator.geolocation.watchPosition(pos=>{p=pos.coords;gps.innerHTML='GPS ✅';if(cal){pts.push([p.latitude,p.longitude]);status.innerHTML='Calibrating '+pts.length+'/10';if(pts.length>=10){let a=[];for(let i=1;i<pts.length;i++)a.push(br(pts[i-1][0],pts[i-1][1],pts[i][0],pts[i][1]));wind=(avg(a)+180)%360;cal=false;status.innerHTML='Calibration Complete';}}let track=(p.heading!=null)?p.heading:null;if(wind!=null&&track!=null){g('track').innerHTML='TRACK '+Math.round(track)+'°';let rel=((track-wind+540)%360)-180;g('rel').innerHTML='RELATIVE ANGLE '+Math.round(rel)+'°';if(Math.abs(rel)>135){g('mode').innerHTML='DOWNWIND';}else if(Math.abs(rel)<60){g('mode').innerHTML='UPWIND';if(rel>=0){g('tack').innerHTML='PORT';port.push(track);if(port.length>30)port.shift();}else{g('tack').innerHTML='STARBOARD';star.push(track);if(star.length>30)star.shift();}}else{g('mode').innerHTML='REACHING';g('tack').innerHTML='TRANSITION';}}redraw();},e=>alert(e.message),{enableHighAccuracy:true});calBtn.onclick=()=>{if(!p)return alert('Start GPS first');pts=[];cal=true;status.innerHTML='Sail downwind...';};